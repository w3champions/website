version: '3.1'
services:
  statistic-service:
    image: modmoto/w3champions-statistic-service-dev:latest
    container_name: w3champions-statistic-service-dev-dev
    restart: always
    environment:
      - VIRTUAL_HOST=statistic-service-test.blood-bowl-league.com

networks:
  default:
    external:
      name: nginx-proxy
